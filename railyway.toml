[build]
builder = "railpack"
# Build command: compile Medusa & install server deps
buildCommand = "npm run build && cd .medusa/server && npm ci --omit=dev --prefer-offline --no-audit --no-fund"

[cache]
# Cache the runtime node_modules to speed up subsequent builds
paths = [
  ".medusa/server/node_modules"
]